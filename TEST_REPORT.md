# TEST REPORT

最終実行日は2026-08-22（JST）です。すべての検査はローカル開発サーバーで実行し、公開は実施していません。

| 区分 | 実行内容 | 結果 |
| --- | --- | --- |
| 型検査 | `pnpm check` | PASS |
| ルール単体検査 | `pnpm test` | PASS — 21件 |
| 問題生成 | `node scripts/generate-puzzles.mjs` | PASS — 88件 |
| 問題一覧 | `node scripts/validate-levels.mjs` | PASS — 88件 |
| ブラウザ操作 | `pnpm exec playwright test` | PASS — Chromium 7件、WebKit 7件 |
| 本番ビルド | `pnpm build` | PASS |

## 単体検査の内訳

キューブの1マス前進、MARKの一致判定、AREA 3×3判定、複数AREAの同時処理、MARK上VOIDの保護、取り逃し上限の足場崩壊、VOID捕獲時の足場損失、3種のPERFECT得点、通常・範囲捕獲得点、MIND INDEXの上限下限、状態の独立複製、88件のデータ読込と解法再生、IDとシードの一意性を検査しました。加えて、手前下辺支点の回転軌跡、回転区間の通過体積、回転体積に対するプレイヤー衝突、登録済み解法の移動可能性、必要キューブ全回収、VOID非捕獲、規定回転数の再生一致を検査しました。

## ブラウザ操作検査の内訳

ChromiumとWebKitを用い、1280×720でタイトルからCampaignを開始し、HUDの表示、PAUSE／RESUMEを確認しました。CREATEではセル変更、localStorage保存、IMPORTとMIRRORの操作面を確認しました。TUTORIALとDUELでは専用盤面・手番HUDの開始を確認しました。Campaignでは設定の保存、プレイ状態の保存、再読み込み後のPAUSED復帰を確認しました。さらに、プレイヤーを床外へ移動させた際に`FALL INTO VOID`の即時ゲームオーバーへ遷移することを操作で検証しました。400×870では仮想スティック、AREA、FAST、MARK / CAPTUREの表示を確認しました。870×400では横画面の仮想スティック、FASTのpointerdown/pointercancel、AREA、盤面、HUDを検証しました。

## 目視確認

以下のビューポートで`?demo`を表示し、固定視点、盤面タイル、巨大キューブ、プレイヤー、HUD、画面端情報、モバイル操作面を確認しました。

| サイズ | 目的 | 結果 |
| --- | --- | --- |
| 1280×720 | タイトル、盤面、HUD、キューブのスケール | PASS |
| 400×870 | 縦画面の盤面と下部タッチ操作 | PASS |
| 870×400 | 横画面の盤面占有率と情報表示 | PASS |

## 修正履歴

最初の3D確認でBabylon.jsの`InstancedMesh`初期化が不足していたため、必要な副作用インポートを追加しました。また、デモ起動時にタイトルパネルが閉じない状態を修正しました。画像生成の失敗プレースホルダーが画面内に見える状態を避けるため、ランタイムのタイトル背景・ロゴは独自のCSS幾何表現へ切り替えました。問題データはMARKが同時に1つというルールに合わせて必要キューブの奥行間隔を調整し、登録済み攻略手順の再生検査を全88問で通過させました。

監査改修では、キューブの回転を中心自転と平行移動から**手前下辺支点の90度回転**へ置換し、回転中の衝突を通過体積のサンプリングに基づく判定へ変更しました。規定回転数は最初の必要捕獲から最後の必要捕獲までの差分へ変更し、問題JSONを再生成しました。問題検証はヘッドレス再生器を通じ、プレイヤー移動時間、MARK、AREA、VOID非捕獲、必要捕獲、規定回転数を照合します。CREATEには規定回転数、JSON読込、左右反転、保存済み問題読込を追加しました。Campaignはversioned snapshotを自動保存・復帰し、品質・音声設定は3D描画密度とWeb Audioへ即時接続しました。PAUSEコマンドは重複イベントに対して冪等になるよう修正しました。

盤面座標は、プレイヤー側の手前端を`z=0`、奥側を正のZとして統一しました。キューブは正のZから0へ向けて、床タイルの上面を基準に手前側の辺を支点として回転します。プレイヤー座標はクランプせず、足場の有効領域を越えた固定更新で`FALL INTO VOID`へ即時遷移します。反転後の88問は再生成し、ヘッドレス解法検証に全件合格しました。

Stage 5 Wave 2 Puzzle 1は、最初のVEILを手動捕獲してAREAを作り、到着した2つ目のVEILをAREAで連鎖捕獲しながら、同じ範囲のVOIDをMARKで保護する手作業代表問題へ置換しました。捕獲中はキューブが床面へ沈下し、シアンの信号粒子を放出します。足場列を失う時は破片が落下し、弱いカメラ揺れを伴います。演出メッシュは再利用するため、連続捕獲中に個別メッシュを増やしません。

モバイル操作は、固定ジョイスティックを廃止して、**盤面を押した開始地点にのみ出現するフローティング移動キー**へ置換しました。キーを画面上へ動かすと、プレイヤーは盤面の奥方向へ進みます。通常問題・代表問題・チュートリアルのキューブは、初期足場の奥端側から出現し、手前へ転がります。盤面には各マスを区別する高輝度測量グリッド、キューブには種類別の発光輪郭、プレイヤーには進行方向を示す前向きの指標を追加しました。375×812ではフローティング移動キーの出現・前進・解除、870×400では主要アクションとHUDのフォールバック表示を確認し、同一スイートをChromiumとWebKitで通過しました。
