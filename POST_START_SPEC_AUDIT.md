# 開始後操作・キューブ規則・参照仕様差分監査

> **履歴監査:** 後続修正前後の判断経緯を残す文書です。現行仕様は[CURRENT_STATE.md](./CURRENT_STATE.md)、現行検査は[TEST_REPORT.md](./TEST_REPORT.md)を優先してください。

## 監査範囲と方針

本監査は、公開情報とユーザー提示仕様にある**抽象的なゲーム機能**を、CUBIC ORDEALの独自名称・独自美術・独自問題へ適用するために行う。既存作品の名称、画面、問題配置、素材、音楽、演出データは取り込まない。公開情報の出典は`REFERENCE_RULES_RESEARCH.md`に保存した。

## 開始後の操作と状態遷移

| 操作 | 現行挙動 | 受入仕様 | 監査判定 | 対応 |
| --- | --- | --- | --- | --- |
| 移動 | キーボード、ゲームパッド、タッチを固定30Hzの`movePlayer`へ集約する。 | 足場上だけを自由移動し、盤外へ出た場合は即座に落下する。 | 対応済み | 盤外判定、上下左右、ゲームパッドD-padを回帰検査する。 |
| 上側スワイプ | 3D canvasへ直接入り、カメラ角度を変更する。 | 上半分はカメラ専用で、移動入力を生まない。 | 対応済み | 縦画面の上半分に限るE2Eを維持する。 |
| 下側タップ | フローティング移動キーはviewport基準の固定位置へ描画される。 | **画面下部でタップした位置**に移動パッドを表示し、スワイプ方向で移動する。 | 対応済み | 座標属性を使い、タップ地点との一致をE2Eで検証する。 |
| MARK / CAPTURE | MARKを1個だけ置く。対象が上なら捕獲し、対象不在時の再入力はMARKを解除する。AREAアンカー上の通常MARKは拒否する。 | 1個だけ置く。対象到達時は捕獲し、対象不在時の再入力は明示的にMARKを解除する。 | 対応済み | WebKitを含む設置・解除・表示のE2Eで確認する。 |
| AREA | 全VEILアンカーを同時評価し、3×3の対象を処理する。MARK上のVOIDは除外する。 | 全アンカーを一斉に処理し、VEIL連鎖とMARK保護を維持する。 | 対応済み | 連鎖、複数アンカー、保護、禁止対象混入を回帰検査する。 |
| FAST | キー／ゲームパッドは押下中だけ高速化し、モバイルはpointer／touch／mouseの解除・capture喪失・領域外離脱で必ず停止する。 | 押している間だけ高速化し、指が外へ出ても必ず解除する。 | 対応済み | 解除イベントを回帰検査する。 |
| COUNTDOWN | カウントダウン中に到達したMARK／AREA入力は、開始時に消費せず保持する。 | 開始直前の入力を無効化または開始後に確実に処理し、入力を黙って失わせない。 | 対応済み | 状態依存の入力消費と開始後E2Eで確認する。 |
| PAUSE / MENU | Pauseは更新を停止し、結果画面のReturnはWorldをMENUへ遷移してモード選択を開く。 | Pause中はロジック停止。Returnは確実にタイトル／モード選択へ戻る。 | 対応済み | Pause・Resume・ReturnをE2Eで確認する。 |
| PRACTICE | REWIND、SAVE、LOAD、STEPを提供し、Campaign記録とは独立した一時履歴を使う。 | いずれもCampaign最高記録に影響させず、解法段階も確認できる。 | 対応済み | 4操作とバナーをE2Eで確認する。 |

## キューブ・足場・得点の適用規則

| 規則 | 現行挙動 | 受入仕様 | 監査判定 | 対応 |
| --- | --- | --- | --- | --- |
| NORMAL | MARK上で手動捕獲、自然落下はLOSS。 | 全て捕獲し、落下は損失メーターへ計上する。 | 対応済み | 行動別テストを追加する。 |
| VEIL | 手動／AREA捕獲でアンカーを残し、AREAは同時に評価する。 | 必須捕獲、3×3連鎖の起点になる。 | 対応済み | 代表問題で連鎖を確認する。 |
| VOID | 手動／AREA捕獲で即時に1列を失い、MARK上ではAREAから保護される。自然落下は損失なし。 | 捕獲禁止、自然落下が正解、MARK上はAREA保護。 | 対応済み | 単独・AREA・保護の全経路を検査する。 |
| 押し潰し | 未処理の全種類を損失へ計上し、閾値単位の足場削除後に高速落下演出を経てAGAINへ移る。 | 未処理の**全種類**を高速で落下させ、損失・足場削除を解決後に同問をAGAINする。 | 対応済み | 複数列損失、足場不足、AGAINを回帰検査する。 |
| 捕獲停止 | 回転を止め、プレイヤー移動を許可する。 | 次の目的地へ走る時間として使える。 | 対応済み | 停止中の移動と入力をE2Eで確認する。 |
| PERFECT | 必須回収、VOID未捕獲、LOSS 0で足場追加。最初の必須捕獲から最後の必須捕獲までを回転数として扱う。 | 足場追加と3段階の規定回転数得点。 | 対応済み | 3得点帯と計測開始・終了を単体検査する。 |

## 参照作品との差を独自実装として扱う方針

| 区分 | 差異 | 決定 |
| --- | --- | --- |
| 表現 | CUBIC ORDEALはObsidian Observatoryの美術、MIND INDEX、NORMAL／VEIL／VOID名称を使う。 | 独自性を維持する。 |
| カメラ | 公開情報の固定読解視点に対し、ユーザー指示により上半分スワイプの限定カメラ操作を加える。 | 下半分移動と完全分離し、読解を崩さない範囲へ制限する。 |
| 問題 | 独自88問・固定seedを使い、既存問題の配置や攻略データは使用しない。 | 抽象的なNORMAL／VEIL／VOIDの組合せと検査だけを整合させる。 |
| モード | PRACTICE、CREATE、DUEL、RUMなどは独自拡張を含む。 | 参照の機能目的に整合させ、個別の画面やデータは模倣しない。 |

## 完了条件

開始後のすべての操作は、入力 → 状態遷移 → キューブ／足場への適用 → HUD・演出という一貫した経路を持つ。各経路は、ユニットテスト、Chromium／WebKitのモバイルE2E、production E2Eで確認する。画面下部の任意タップ位置には移動パッドが現れ、上側のカメラ操作や右下のアクションボタンと干渉しない。
