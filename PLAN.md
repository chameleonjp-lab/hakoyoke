# Game Plan: CUBIC ORDEAL

> **履歴資料:** 初期実装計画です。完了・未完了の現行判定には使用せず、[CURRENT_STATE.md](./CURRENT_STATE.md)と[todo.md](./todo.md)を優先してください。

## Risk Tasks

### 1. 固定時間グリッド・シミュレーション

- **Why isolated:** 見た目のフレームレートと、キューブ回転、捕獲、足場損失、巻き戻しの判定を分離しないと、入力タイミングや問題検査の再現性が崩れる。
- **Approach:** 30Hz固定ステップで整数グリッドの`GameState`を更新し、描画は直前と現在の状態を補間する。乱数は固定シードのPRNGに限定し、状態スナップショットを巻き戻し・検証に再利用する。
- **Verify:** 低速・高速の描画環境でも同一シードの問題で同じ状態遷移になる。キューブは毎回ちょうど1マス前進し、巻き戻し後も同じスナップショットへ復元する。

### 2. キューブ回転と通過体積の衝突

- **Why isolated:** キューブを単なる平行移動として扱うと重量感がなく、回転中にプレイヤーが通過体積へ入る危険を正しく検出できない。
- **Approach:** 論理上は`z + 1`へ移る離散更新、描画上は手前下の辺を軸にした90度回転とする。衝突は回転中のセル区間とプレイヤーの円形足元を含む広めのスイープ領域で判定する。
- **Verify:** 各キューブの回転軸はタイル境界に固定され、接地で床にめり込まない。回転中に危険セルへ入ったプレイヤーは押し潰され、回転後に安全セルへいるプレイヤーは誤検知しない。

### 3. 88問の決定的生成と解法検証

- **Why isolated:** 大量の問題を手作業で作ると重複・未検証・クリア不能が混ざりやすい。範囲捕獲と禁止キューブ保護の組み合わせも検査が必要である。
- **Approach:** ステージ表どおりのサイズと数を固定し、固有シードごとに「解法スクリプト→逆算配置」の順で生成する。純粋なヘッドレス検証器が各解法を再生し、必須捕獲、禁止キューブ未捕獲、規定回転数、移動余裕を検査する。
- **Verify:** 88件すべてに固有ID、seed、サイズ、難易度タグ、規定回転数、解法、検査結果があり、テストで全件の検証が通る。同一シードの生成結果は同じになる。

### 4. タッチ・キーボード・ゲームパッドの統一入力

- **Why isolated:** pointerup/cancel漏れはモバイルで入力が残り、移動が止まらない事故を生む。Web Audioの初期化も最初の入力に連動する。
- **Approach:** `InputManager`が意味論的アクション（move/mark/area/fast/pause）だけをゲームに供給する。仮想スティックとボタンはpointerdown/up/cancelで管理し、visibilitychange・blurでは入力を全解除する。
- **Verify:** 画面外へ指を移動してもFASTや移動が残らない。WASD・矢印・Space/Z・X/E・Shift/C・Escape、ゲームパッド、タッチで同じ行動を実行できる。

## Main Build

ゲームは巨大キューブが奥から手前へ迫る一本の浮遊足場を舞台にする。プレイヤーは自由に走り、青いMARKを1つ設置して接近キューブを捕獲する。通常キューブとVEILキューブは回収対象、VOIDキューブは通過対象である。VEILを捕獲すると緑のAREA印を残し、AREAで全印が同時に3×3捕獲を行う。青いMARKの下にあるVOIDはAREAから保護される。

タイトル、モード選択、難易度、チュートリアル、キャンペーン、練習、作成、交互対戦、設定、結果画面を実装する。問題と進行、設定、最高記録、自作問題はlocalStorageに保存し、外部API・ログイン・サーバー状態は用いない。音は初回入力で開始するWeb Audio APIの合成音と環境的リズムで作る。

- **Assets needed:** 生成済みのObsidian Observatory基準画像、タイトル背景、シンボルロゴ、玄武岩タイル、HUDパネル。3Dのプレイヤー・キューブ・粒子は基本形状とプログラム素材で構成する。
- **Verify:**
  - 移動方向とキャラクターの向きが一致し、待機→走行→設置→踏ん張り→転倒が滑らかに遷移する。
  - 青いMARKは常に1つで、MARK上のキューブだけを手動捕獲する。
  - VEIL捕獲はAREA印を残し、複数AREAが同時作動する。MARK上のVOIDはAREAから捕獲されない。
  - 取り逃しとVOID捕獲で足場が減り、完全捕獲で足場が増える。規定回転数とスコアが正しい。
  - 盤面の可読性、キューブの種類、カメラ角度、HUD密度が視覚基準の配色・構図に沿う。
  - デスクトップ、400×870縦、870×400横、小型スマートフォン相当で重要操作に到達できる。
  - ブラウザコンソールに未処理例外、欠損テクスチャ、入力残りがない。
  - `?demo`で決定的な自動操作を行い、実際のゲームプレイをスクリーンショットで確認できる。

