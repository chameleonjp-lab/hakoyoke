# I.Q.との差分監査・今後の実装基準レポート

- 対象リポジトリ: `chameleonjp-lab/hakoyoke`
- 監査日: 2026-08-27
- 対象ブランチ: `main`
- 対象コミット: `add9e95dd339b9254a21a084d61b3b5018f24fe3`
- 比較対象: 初代『I.Q Intelligent Qube』
- 目的: 元作の名称、画像、音楽、問題配置を複製せず、元作の面白さを生む規則と操作のリズムをどこまで実装できているか確認し、今後の実装順を固定する

## 1. 監査方法と限界

現在の `main` にあるゲーム処理、描画、入力、問題生成、保存、画面、テスト、最新のGitHub Actionsを確認した。元作側はPlayStation公式説明書を最優先とし、公式紹介、開発者インタビュー、専門的なプレイ資料を補助に使った。

この監査はコードと公開資料の照合が中心である。元作と現実装を同じ機器で計測した比較ではないため、移動速度、入力猶予、カメラ距離、音量などの最終値は実機確認で決める必要がある。一方、座標処理、状態の初期化、得点の加算位置など、コードから確定できる差分は断定している。

### 主な参照資料

- [PlayStation公式説明書](https://secure.cdn.us.playstation.com/manuals/classic/games/intelligent-qube-manual-en.pdf)
- [PlayStation公式紹介](https://store.playstation.com/en-hk/concept/10004410)
- [PlayStation公式記事](https://blog.playstation.com/2024/11/04/as-seen-in-astro-bot-10-playstation-vip-bot-cameos-and-the-games-that-inspired-them/)
- [1997年の開発者インタビュー英訳](https://shmuplations.com/iq/)
- [Hardcore Gaming 101の作品解説](https://www.hardcoregaming101.net/intelligent-qube/)
- [Speed Demos Archiveの詳細な規則解説](https://speeddemosarchive.com/IntelligentQube.html)
- 現実装の主要資料: [README.md](./README.md)、[CURRENT_STATE.md](./CURRENT_STATE.md)、[TEST_REPORT.md](./TEST_REPORT.md)、[LEVEL_VALIDATION_REPORT.md](./LEVEL_VALIDATION_REPORT.md)

## 2. 結論

現在の実装には、固定時間で進む規則処理、キューブの回転軌道を使った当たり判定、3種類のキューブ、床の印、3×3範囲捕獲、問題データ検査、スマートフォン入力、保存復帰、継続的な自動検査がある。技術的な土台は整理されており、単なる画面試作ではない。

しかし、元作の中心である次の循環は、まだ再現できていない。

> 上手く消すと足場が延び、次の問題で考える時間が増える。失敗すると足場が縮み、同じ速さのキューブでも次から厳しくなる。

現実装では、足場の列数を増減してもキューブの出現位置と落下位置が変わらない。したがって、PERFECTの列追加と失敗時の列喪失が、次の問題の猶予時間を変えない。見た目の足場だけが伸び縮みし、ゲームの難しさへつながっていない。

さらに、元作はキューブが印の上で回転している最中に捕獲するが、現実装は回転中の捕獲を拒否する。範囲捕獲から印付きの床を守る規則も、元作はキューブの種類を問わないのに対し、現実装はVOIDだけを守る。問題間で保持すべき失敗目盛りや範囲捕獲の印も、現実装では問題ごとに消える。

そのため、現状は「元作の主な部品を備えた独自パズル」ではあるが、「元作に近い駆け引きと緊張の推移」には達していない。今後は見た目の追加より先に、足場、問題間状態、捕獲時間帯を直す必要がある。

## 3. 現在できていること

以下は今後も残す価値が高い。

1. **規則処理と3D描画を分離している。** `GameWorld` が30回/秒の固定間隔で状態を決め、Babylon.js側は結果を描く構成になっている。再現性のある検査を作りやすい。
2. **キューブの回転軌道で押し潰しを判定している。** 単純な着地点だけでなく、手前下辺を支点に90度回る途中の体積を検査している。
3. **入力経路が広い。** キーボード、ゲームパッド、スマートフォンの移動、MARK、AREA、FAST、PAUSEを用意し、指が外れた場合や画面が隠れた場合の解除も実装している。
4. **問題データの破損を検出できる。** 88問の件数、配置、登録解法、VOID非捕獲、AREA消費などを自動確認している。
5. **独自作品としての境界を守っている。** 名称、画面、問題配置、画像、音楽、評価式をそのまま複製せず、`CUBIC ORDEAL`として別の表現を採用している。

最新の `main` に対するGitHub Actionsは成功している。ただし、現在の自動検査は「現実装で定めた規則に矛盾がないか」を主に確認する。元作と同じ規則か、人が88問を実際に走って解けるか、足場増減で次の猶予が変わるかまでは保証していない。

## 4. 重大差分の一覧

公開や完成判定より先に直す項目を **P0**、その後に直す重要項目を **P1**、仕上げ段階の項目を **P2** とする。

| 優先度 | 分野 | 現在の差分 | 影響 |
| --- | --- | --- | --- |
| P0 | 足場の増減 | `platformRows`を変えても出現位置と落下境界が固定 | 成功と失敗が次の難しさを変えない |
| P0 | ステージ間の足場 | 列数をステージ開始時に戻さず全88問へ持ち越す | 完璧に進むほど足場が異常に長くなり、画面が遠ざかる |
| P0 | 問題間の状態 | 失敗目盛り、MARK、AREAを問題ごとに初期化 | 元作にある問題をまたぐ管理が消える |
| P0 | 捕獲時間帯 | 回転中は捕獲を拒否し、着地後だけ許可 | 解法、入力のリズム、反射判断が別物になる |
| P0 | AREA中の保護 | MARKが守るのはVOIDだけ | NORMALやVEILを意図的に残す元作の技が使えない |
| P0 | ゲームオーバー後 | 終了状態を保存するが、続行、新規開始、保存消去の導線がない | CAMPAIGNが終了画面へ戻り続ける可能性がある |
| P1 | AREAの移動中判定 | 回転中も発動できるが、回転前の整数座標で対象を選ぶ | 見えている位置と捕獲対象がずれる |
| P1 | ステージ得点 | 残り列×1,000点を最終完走時にしか加えない | 元作の各ステージ終了得点と異なる |
| P1 | 問題の流れ | 各問題の後に操作待ちの結果画面を出す | 元作の連続した圧力が切れる |
| P1 | カメラと移動 | カメラを回せる一方、タッチだけカメラ基準で、キーボードとゲームパッドは盤面基準 | 同じ見た目でも入力方向が機器で変わる |
| P1 | 難易度 | 高難易度ほどキューブを速めるだけでなく、人物を遅くし、捕獲停止も短くする | 画面説明の「変わるのは時間だけ」と一致しない |
| P1 | 問題設計 | 88問を1つの生成規則から作り、88問すべてでAREAを使う | 数は多いが考え方が似やすく、学習順も単調になる |
| P1 | 検査方法 | 登録手順の再生は、実際の人物移動、押し潰し、入力時間を完全には通さない | PASSでも人が安定して解けるとは限らない |
| P2 | 描画更新 | 規則も人物位置も30回/秒で更新し、表示側の補間がない | 60Hz以上の画面で動きが段階的に見える可能性がある |
| P2 | 音 | 合成した短い効果音だけで、足音、継続する重量音、BGM、声がない | 元作の圧迫感と成功時の満足感が弱い |
| 維持 | 独自要素 | PRACTICE、巻き戻し、即時CREATE、独自名称、独自評価式 | 元作の複製を避けつつ遊びやすさを増やしている |

## 5. ゲームロジックの詳細

### 5.1 足場の列がゲームへ影響していない

元作の公式説明書では、禁止キューブを消すと最後の列が崩れ、PERFECTでは列が1つ増える。さらに、問題の長さが足場より長くなるとゲームオーバーになる。列数は表示用ではなく、キューブが端へ届くまでの移動回数そのものである。

現実装は次の構造になっている。

- キューブは問題データ内の固定された `spawnRow` から始まる。
- 1回転ごとに `z` を1減らし、`z < 0` で落下する。
- `platformRows` は床を何列描くかと、人物が歩ける最大 `z` を決める。
- `platformRows` を増減しても、`spawnRow` と `z < 0` は変わらない。

このため、列を増やしてもキューブの移動回数は増えず、列を失っても早く落ちない。列喪失で消えるのはキューブが落ちる側ではなく、固定座標の大きい側である。

加えて、問題を読み込む際のゲームオーバー判定は `depth + 2` を使い、`spawnRow` を含めていない。たとえば奥行9、出現位置5の問題は最大 `z=13` まで使うが、足場11列でも即座には終了しない。床のない場所にキューブが存在する状態を許してしまう。

#### 必要な修正

問題データは「問題内の相対位置」だけを持ち、実際の出現位置は現在の足場長から計算する。足場が1列増えたら、次の問題が端へ届くまでにちょうど1回転多く必要になり、1列失ったら1回転少なくなる構造へ変える。

実装方法は次のどちらかに統一する。

- 落下端を固定し、現在の列数から問題の出現位置を計算する。
- 足場の先端位置を別の値として持ち、列喪失時に落下端を1列分進める。

前者の方が、描画と問題データを分けやすい。

### 5.2 足場をステージ間で持ち越している

`loadPuzzle(..., false)` は得点と `platformRows` を残す。ステージが変わっても `false` のままであるため、列数は全88問へ持ち越される。

開始時が12列で、88問をすべてPERFECTにすると、静的計算では最終的に100列になる。現在のカメラ計算では距離が約12から72まで広がり、盤面と人物が大きく縮んで見える。元作の「各ステージに定められた足場で開始し、そのステージ内の成否で列が動く」という構造とは異なる。

#### 必要な修正

`STAGE_PLAN` に各ステージの基準列数を持たせる。ステージ開始時にその値へ設定し、PERFECTと失敗による増減は少なくともそのステージ内だけに限定する。元作の正確な列数をそのまま使う必要はない。スマートフォンで人物と床が読める距離を優先し、同じ因果関係を再現する。

### 5.3 失敗目盛りを問題ごとに消している

元作の失敗目盛りであるBlock Scaleは、通常キューブや緑のキューブを落とすたびに増え、横幅と同数の失敗で1列を失う。詳細なプレイ資料では、目盛りは問題ごとではなくWAVEの境界などで戻るとされている。

現実装は問題を読み込むたびに `initialStats()` を作り直すため、`misses` が0へ戻る。横幅4なら、各問題で3個ずつ落としても列を失わずに先へ進める。元作の「小さな失敗が次の問題まで残り、あと1個で列を失う」という緊張が消える。

#### 必要な修正

記録を次の2層に分ける。

- 問題ごとに戻す値: PERFECT可否、捕獲数、VOID捕獲数、回転数
- WAVEまたはステージ内で残す値: Block Scale、足場列、得点、保持中のAREA

どの境界で戻すかを仕様書に明記し、その境界以外では初期化しない。

### 5.4 AREAの印を問題ごとに消している

現実装は問題読み込み時に `areas = []` とする。元作の専門的なプレイ資料では、緑の印は次の問題やWAVEへ残り、後の問題で使う判断が発生する。現実装は各問題内でVEILを捕獲し、その問題内で使い切る設計であるため、問題をまたぐ資源管理がない。

この差は単なる保存の有無ではない。元作では「今使うか、次へ残すか」が解法の一部になる。現実装では88問すべてにAREAを置き、82問で2回以上のAREA連鎖を使うため、ほぼ毎回同じ種類の解法へ寄る。

#### 必要な修正

AREAを保持する範囲を先に決める。元作に近づけるなら、同一ステージ内の問題とWAVEをまたいで残し、床の作り直しや範囲外化が起きたときだけ消す。その後、複数問題を連続で再生する検査へ置き換える。

### 5.5 捕獲する時間帯が逆になっている

公式説明書は、キューブが印の上で**回転している最中**に捕獲ボタンを押すと説明している。現実装は `isRolling` の間に入力すると `WAIT FOR LANDING` を表示し、着地後の停止時間だけ捕獲できる。

この差で次が変わる。

- 元作では回転の見た目を読んで押すが、現実装では着地音の後を待つ。
- 元作の短い反射判断が、現実装では停止区間の確認作業になる。
- タッチ画面は回転中でも対象があると `CAPTURE` を表示するが、押すと拒否される場合がある。
- 回転数、FAST、難易度を調整しても、捕獲窓が別物なので元作の感覚へ近づかない。

#### 必要な修正

キューブの論理位置を「回転前のマス」だけで持たず、回転先と進行率を含める。印のマスをキューブの回転体積が覆っている時間帯を捕獲可能時間とし、入力がその時間内なら捕獲する。正確な開始率と終了率は、公式説明と実機動画を使って別途計測する。

### 5.6 AREAだけが回転中に古い位置を使う

手動捕獲は回転中に拒否する一方、AREAは回転中でも発動できる。ところが、対象選択は回転が終わるまで更新されない整数の `cube.z` を使う。このため、画面では次のマスへ倒れているのに、判定は前のマスにいる場合がある。

#### 必要な修正

手動捕獲とAREAで同じ「現在どの床を覆っているか」という判定を使う。回転中の論理位置を1つの関数にまとめ、描画、押し潰し、MARK、AREAが同じ結果を見るようにする。

### 5.7 MARKによる保護がVOID限定になっている

公式説明書では、AREA内に通常のMARKがある場合、その床はAREAから外れ、その上のキューブは消えない。キューブの種類は限定していない。

現実装の `isProtectedVoid()` は、MARKと一致したうえで `cube.type === "void"` の場合だけ除外する。NORMALやVEILにMARKを置いてもAREAで捕獲される。

#### 必要な修正

保護判定をキューブ種類から切り離し、MARKの床と一致したキューブをAREA対象から外す。VOID保護はその利用例の1つとしてチュートリアルで教える。

### 5.8 ステージ終了得点が最終結果にしか入らない

公式説明書では、各ステージを終えるたびに残り列数×1,000点を加える。現実装は次の問題が存在しない場合、つまり全88問終了時に1回だけ加える。

#### 必要な修正

ステージ番号が変わる直前に1度だけ加算する。保存復帰やCONTINUEの連打で重複加算しない識別子を持たせる。最終ステージも同じ処理を通し、特別扱いを減らす。

### 5.9 GAME OVER後の続行がない

元作はゲームオーバー後、現在のステージから続行できる代わりに得点を失う。現実装は `GAME_OVER` をCAMPAIGN保存へ残し、結果画面にはメニューへ戻る操作しかない。タイトルからCAMPAIGNを選び直すと、保存された `GAME_OVER` を再表示する可能性がある。新しいCAMPAIGNを始める操作もない。

#### 必要な修正

結果画面に次を明示する。

- `CONTINUE STAGE`: 現在ステージの先頭から再開し、得点とそのステージの状態を仕様どおり戻す
- `NEW CAMPAIGN`: 保存を消して最初から開始する
- `RETURN TO MENU`: 保存状態を変えずに戻る

続行時に何を残すかは独自仕様でもよいが、終了状態から抜けられない状態は許可しない。

## 6. 操作感と時間設計

### 6.1 元作の基本操作との対応

元作の公式操作は、方向キーで移動、四角でキューブ加速、バツでMARKと捕獲、三角でAREA、STARTで停止である。現実装はキーボード、ゲームパッド、タッチへ置き換えつつ、操作の役割はおおむね対応している。

一方、元作は方向キーを前提とした固定された見え方で、現実装はアナログ移動と回転可能なカメラを組み合わせる。現実装のタッチ移動はカメラ方向へ合わせるが、キーボードとゲームパッドは盤面の固定方向を使う。カメラを回すと、同じ画面方向への入力結果が機器で変わる。

#### 方針

元作に近い読みやすさを優先するなら、プレイ中のカメラ回転と拡大縮小を止める。独自作品としてカメラ操作を残すなら、全入力をカメラ基準へ統一し、誤操作しない専用操作へ分離する。スマートフォンでは前者を推奨する。

### 6.2 難易度が人物速度まで変えている

画面は「配置は変わらない。変わるのは、読み終えるまでに残された時間」と説明する。しかし実際には、高難易度ほどキューブの1周期が短くなり、人物速度も遅くなり、捕獲後の移動時間も短くなる。

| 難易度 | 1回転周期 | 人物速度 | 1周期で移動できる概算距離 |
| --- | ---: | ---: | ---: |
| BEGINNER | 1.87秒 | 4.80 | 8.98マス |
| EASY | 1.62秒 | 4.65 | 7.53マス |
| NORMAL | 1.38秒 | 4.45 | 6.14マス |
| HARD | 1.14秒 | 4.25 | 4.85マス |
| EXTREME | 0.98秒 | 4.10 | 4.02マス |

EXTREMEではBEGINNERの半分未満しか1周期内に移動できない。キューブだけを速める以上に難しさが増えている。

#### 方針

まず人物速度を全難易度で共通にし、キューブ周期だけを変える。人物速度を変える場合は別の人物特性や設定として見せ、難易度説明にも書く。`assistance` の値は現在のゲーム処理で使われていないため、不要なら削除し、使うならMARK案内や速度警告など具体的な補助へ結び付ける。

### 6.3 FASTが停止区間も短くする

元作の説明はキューブの回転を速めるとしている。現実装は回転時間だけでなく、着地後の停止時間も1.82倍で進める。結果として、FAST中は捕獲可能な着地時間まで短くなる。

捕獲を回転中へ戻した後、FASTが回転だけを速めるのか、次の回転開始まで含めて全体を速めるのかを仕様として固定する。入力練習では通常速度とFAST時の捕獲窓を別々に検査する。

### 6.4 30回/秒の規則更新を表示へそのまま使っている

固定30回/秒は再現性のある規則処理に向く。一方、人物位置とキューブ進行率も30回/秒でしか変わらず、60Hzや120Hzの画面では同じ位置を複数フレーム表示する。精密な捕獲ゲームでは、入力遅延と見た目の段差として感じる可能性がある。

規則は30回/秒のままでもよいが、描画は直前状態と現在状態の間を滑らかにつなぐ。捕獲入力は時刻付きの待ち行列にし、1回の更新間に複数入力があっても潰さない。

## 7. 問題設計の差分

元作の開発者インタビューでは、問題ごとに考え方の名前を付け、紙で設計し、別の担当者が繰り返し遊んで採用を決めたと説明されている。配置の数よりも、各問題に異なる考え方があることを重視していた。

現実装の88問は壊れていない。ID、seed、配置、登録解法はすべて検査を通る。しかし、生成処理は次の共通形を使う。

- 中央付近の3列を主な捕獲対象にする
- 中央へVEILを一定間隔で置く
- 外側をVOID中心にする
- 端へ1本または2本の手動捕獲経路を追加する
- ほぼすべてをAREA連鎖で処理する

`LEVEL_VALIDATION_REPORT.md`では、88問すべてがAREAを使い、82問がAREAを2回以上使う。数値上は別問題でも、「中央の連鎖を起こして端を補う」という同じ考え方へ寄りやすい。

また、現在の解法検査は、回転番号ごとの行動を並べ、人物が直線距離として間に合うかを概算する。実際の `GameWorld` を通して押し潰し、迂回、入力猶予、カメラ、端からの落下まで再生していない。

### 今後の問題設計方針

元作の配置を写さず、独自の問題原型を作る。最低でも次の考え方を分ける。

1. MARKだけで順番を読む問題
2. 1つのAREAを遅らせて使う問題
3. 複数AREAを同時に使う問題
4. MARK保護で1マスだけ残す問題
5. 左右へ大きく走る問題
6. AREAを次の問題へ残す問題
7. 足場が短いと解法を変える問題
8. FASTを使うと安全になる問題

各問題には短い設計意図、想定する学び、代替解法、最短回転数を記録する。自動生成は左右反転や難易度違いなど補助に限定し、採用単位は人が遊んで決める。

## 8. 画面、音、演出の差分

### 8.1 残せている雰囲気

暗い空間、長い床、巨大なキューブ、小さな人物という対比は保たれている。キューブの種類も色と輪郭で見分けられる。元作の画像や名称をそのまま使わず、独自の観測施設という表現へ置き換えている点は適切である。

### 8.2 情報量は元作より多い

元作は必要な数字と床の印を中心にした簡素な画面である。現実装は四辺の計器枠、座標、左右の多数の値、下部パネルを常時表示する。独自性はあるが、スマートフォンでは盤面の読み取りより外枠が目立つ可能性がある。

AREAも、元作は中心の緑の床を残し、発動時に周囲を示す。現実装は約3×3の範囲全体を常時塗るため、範囲は分かりやすいが、考えて覚える負担を減らしている。

#### 方針

通常表示は得点、回転数、失敗目盛り、残り列、PAUSEへ絞る。詳しい値は設定で表示する。AREAは中心だけを常時表示し、範囲は押した瞬間または練習補助で示す。

### 8.3 音の役割が不足している

元作は足音、キューブの連続した重量音、着地、崩落、声、壮大な音楽で、画面外の動きと成功を伝える。現実装はWeb Audioの単発の発振音だけで、BGM、足音、連続する転がり、音声がない。

元作の音源や旋律を使わず、次を独自に作る。

- 歩行速度に同期する足音
- 回転の開始から着地まで続く低い重量音
- キューブ数に応じて厚みが変わる着地音
- MARK、手動捕獲、AREA、VOID、列崩落を聞き分けられる音
- PERFECT、AGAIN、GAME OVERに相当する独自の短い音声
- 問題の圧力を邪魔しない独自BGM

音は飾りではなく、次に押す時刻を知らせる手掛かりとして検査する。

## 9. 独自要素として残すもの

次は元作と違っていても問題ではなく、むしろ残すべきである。

- `CUBIC ORDEAL`、NORMAL、VEIL、VOIDなどの独自名称
- 独自の人物、床、キューブ、画面、画像、色設計
- 元作の問題配置を使わない88問以上の独自問題
- PRACTICEの巻き戻し、クイックセーブ、1回転送り
- CREATEのJSON入出力と即時テスト
- スマートフォン専用操作
- 独自のMIND INDEX。ただし計算根拠は画面や説明書で公開する
- 元作と異なる演出、音楽、声

DUELは意図的な別物として扱う。元作の2人用は交互に遊んで得点を比べる形式とされるが、現実装はPERFECTで1点、先に5点かつ2点差で勝つ。どちらかへ寄せる必要はない。ただし「I.Qの2人用再現」とは説明せず、独自対戦規則として明記する。

## 10. 実装順

### 第1段階: 規則の正本を固定する

先に文書と検査を作り、コードだけを先行させない。

- 足場の先端、出現位置、落下位置の関係
- ステージ開始列数
- Block Scaleを戻す境界
- MARKとAREAを残す境界
- 回転中の捕獲可能時間
- GAME OVER後のCONTINUEとNEW CAMPAIGN
- ステージ得点の加算時点

### 第2段階: 足場と進行を作り直す

- 問題配置を相対座標へ変換
- 現在の足場から出現位置を計算
- 列増減で次の落下までの回転数が1だけ変わるようにする
- ステージ開始時に基準列へ戻す
- 問題が足場へ収まらない場合の終了判定を実配置から計算
- 保存形式を更新し、古い保存を安全に破棄または変換

### 第3段階: 問題間状態と得点を直す

- 問題記録とWAVE記録を分離
- Block ScaleをWAVE中で保持
- AREAと必要ならMARKを問題間で保持
- 各ステージで残り列得点を1度だけ加算
- CONTINUE、NEW CAMPAIGN、保存消去を追加
- 結果画面の操作待ちを設定で省略し、通常CAMPAIGNは短い自動遷移にする

### 第4段階: 捕獲と入力を直す

- 回転先と進行率を含む占有判定を追加
- 回転中の手動捕獲を実装
- AREAも同じ占有判定を使う
- MARK保護を全キューブへ適用
- カメラを固定、または全入力を同じ基準へ統一
- 難易度から人物速度低下を外す
- 表示補間と入力時刻の保持を追加

### 第5段階: 問題を作り直す

- MARKだけで学ぶ序盤を追加
- AREAを段階的に導入
- 問題をまたぐAREA管理を導入
- 1つの生成式から複数の設計原型へ移行
- 実際の `GameWorld` で人物を動かす解法再生を追加
- iPhone実機で各段階を最低3回ずつ確認

### 第6段階: 音と画面を仕上げる

- 盤面を優先した簡素なHUD
- 独自BGM、足音、連続する重量音、声
- 捕獲窓が音だけでも分かる調整
- 縦画面、横画面、低負荷設定で視認性と遅延を測る

## 11. 完了条件

次をすべて自動検査または実機確認で満たすまで、元作に近いゲーム性の完成とは判定しない。

1. PERFECTで列を1つ増やすと、次の問題が落下するまでの回転可能数が正確に1増える。
2. VOID捕獲またはBlock Scale超過で列を1つ失うと、次の問題の猶予が正確に1回転減る。
3. ステージ変更時に規定の基準列へ戻り、前ステージの列数が持ち越されない。
4. 問題の実配置が足場へ収まらない場合、開始前または規定の演出後に正しくGAME OVERになる。
5. Block Scaleが同じWAVE内の複数問題で残り、規定の境界だけで戻る。
6. AREAが仕様で定めた問題境界を越えて残る。
7. キューブがMARK上で回転している規定時間内に押すと捕獲でき、早過ぎる入力と遅過ぎる入力は捕獲しない。
8. AREAを回転中に押しても、見えている床位置と捕獲対象が一致する。
9. MARKした床は、NORMAL、VEIL、VOIDのどれでもAREAから保護される。
10. 各ステージの残り列得点が1回だけ加算され、再読込や連打で重複しない。
11. GAME OVERからCONTINUEとNEW CAMPAIGNの両方を選べる。
12. カメラを回せる場合、タッチ、キーボード、ゲームパッドの移動方向が同じ見え方になる。固定する場合、プレイ中に誤って回らない。
13. 88問すべてを実際の `GameWorld`、人物速度、押し潰し、入力時間を通して再生できる。
14. 60Hz以上の画面で人物とキューブが30Hzの段差として目立たない。
15. iPhoneの指外れ、通知、画面非表示、長押し、複数指の後に移動やFASTが押しっぱなしにならない。

## 12. 担当の分け方

既定の実装組織に合わせ、次の分担を推奨する。

- 日常実装と反復検査: Luna・Max
- 足場座標、問題間状態、保存形式の複雑な検査: Luna・Max
- 原因不明、保存破損、重大な規則判断: Sol・Extra High
- 各段階の設計判断と最終判断支援: Sol・High
- 公開前の独立レビュー: Sol・High
- 作品としてどこまで元作へ寄せるかの最終決定: ユーザー本人

第2段階と第4段階は同じ担当者だけで完了判定せず、別担当の検査を必須にする。

## 13. 最終判断

今後の目標は、元作をそのまま作り直すことではない。残すべきなのは、単純な規則から生まれる次の3点である。

- 動いているキューブを読んで、短い時間内に押す感覚
- 成功すると余裕が増え、失敗すると次が厳しくなる足場の循環
- 1問だけでなく、その後まで考えてMARKとAREAを管理する判断

現在は1つ目が着地待ちへ変わり、2つ目が見た目だけになり、3つ目が問題ごとの初期化で消えている。ここを直せば、独自の画面、独自問題、スマートフォン操作を保ったまま、元作に近い緊張と達成感へ近づけられる。

したがって、次の実装は**足場座標と問題間状態の再構築**から始める。音、追加問題、画面装飾、対戦拡張はその後に行う。
