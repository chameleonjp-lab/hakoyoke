# モバイルカメラ・移動操作・描画品質の復旧方針

## 操作領域

画面上半分はBabylon canvasへポインターイベントを通し、スワイプによるArcRotateCameraの視点移動だけを許可する。画面下半分だけをフローティング移動キーの領域とし、アクションボタン以外の下半分タッチは移動入力になる。

## 方向規約

移動キーは画面基準で解釈する。上方向は画面奥、下方向は画面手前、右方向は画面右、左方向は画面左へキャラクターを進める。カメラを回した後も規約を維持するため、GameCanvasがカメラの水平forward/right基底を通知し、移動キーのベクトルをその基底でゲーム座標へ変換する。ゲームロジックは変換済みの世界座標だけを受け取り、状態遷移・衝突・落下判定を変更しない。

## 描画品質

アンチエイリアスと端末の表示密度を有効にし、品質設定に応じて描画解像度・影解像度・グローを調整する。床とキューブには明確な輪郭・接地陰影・控えめな発光を保持し、読みづらいノイズや低解像度の見た目を優先しない。

## 受入条件

縦画面では上半分スワイプがカメラを動かし、下半分スワイプだけがキャラクターを移動させる。カメラの向きにかかわらず、移動キーの画面上方向とキャラクターの画面上進行方向は一致する。Chromium、WebKit、productionでタッチ入力、3D盤面、影・グロー、落下ゲームオーバー、Campaign復帰が機能する。

## 実装時の描画初期化修正

床とキューブの輪郭を明確化する`enableEdgesRendering()`には、`@babylonjs/core/Rendering/edgesRenderer`の副作用インポートが必要である。外部プレビューで`EdgesRenderer needs to be imported before`という初期化例外を確認し、scene初期化時に同モジュールを明示的に読み込むよう修正した。
