# CUBIC ORDEAL 要件差分・シニアデバッグ監査報告書

> **履歴監査:** この判定は大規模修正前のスナップショットであり、現行状態ではありません。未対応欄の多くは後続コミットで変更されています。現在の達成状況は[CURRENT_STATE.md](./CURRENT_STATE.md)、検査範囲は[TEST_REPORT.md](./TEST_REPORT.md)を優先してください。

**監査日:** 2026-08-22（JST）  
**監査対象:** 初回要求 `Pasted_content.txt`、現行プロジェクト `cubic-ordeal`、テスト・ビルド証跡  
**監査方法:** 要件トレーサビリティ、ゲーム状態遷移・描画・生成器・検証器・UI・テストコードの静的読解、既存のデスクトップ／モバイル目視結果、Vitest・Playwright・本番ビルド実績の照合。

> **結論:** 現行版は「独自の3Dリアルタイム・キューブ処理ゲーム」としては動く土台を持っています。しかし、元要件が求めた**完成版**としては未達です。最大の問題は、回転と衝突の中核表現、88問の品質保証、TUTORIAL／CREATE／DUEL／保存の実効性、画面操作検査の網羅性です。現状の完成ラベルは撤回し、**検証済みプロトタイプ**として扱うのが適切です。

## 1. 監査上の判定基準

| 判定 | 意味 |
| --- | --- |
| **達成** | 元要件の利用者価値と技術条件が実装・検査の両面で確認できる。 |
| **部分対応** | 基本的な入口はあるが、必須の例外、品質、体験、検査のどれかが欠ける。 |
| **未対応** | UI文言、型、未接続設定、または限定的なコード片のみで、要求機能として成立しない。 |
| **根拠不足** | 実装の存在は確認できるが、要求された実プレイ／自動検査で立証されていない。 |
| **保留（ユーザー指示）** | 元要件にはあるが、途中で「公開などは一旦無視」と明示されたため、今回の未実施は意図的である。 |

## 2. エグゼクティブ評価

現在の強みは、固定30Hzの論理更新、整数グリッド状態、WebGLの3D盤面、3種キューブの基本ルール、MARK、AREA 3×3、VOID保護、88件JSON、基本HUD、モバイル操作の外形、静的ビルドです。これらは「見た目だけのモック」ではありません。

一方で、もっとも重要な「巨大キューブが辺を支点に転がり、プレイヤーが回転中の通過体積を読んで逃げる」部分が、現実装では中心回転と前進の組み合わせ、距離閾値の衝突近似に留まっています。したがって、ゲームの中心的な緊張感と空間パズル性は、要求水準を満たしたとは言えません。また、問題生成器と検証器は「登録手順に合う配置」を確認するだけで、実際のGameWorldを用いる再生・移動時間・AREA連鎖・VOID保護・失敗分岐の検証を行っていません。

## 3. 要件トレーサビリティ・マトリクス

| 要件群 | 判定 | 現行の根拠 | 主な差分・リスク |
| --- | --- | --- | --- |
| 独自名称・素材・問題 | **部分対応** | `CUBIC ORDEAL`、Web Audio合成音、独自JSONを使用。 | 独自性はおおむね担保されるが、独自素材一覧・利用条件の成果物がない。 |
| Vite／TypeScript／React／静的Web | **達成** | Vite/React/TypeScript構成、バックエンドなし。 | なし。 |
| Three.jsを直接制御 | **未対応** | 実装はBabylon.js。 | 元要件はThree.jsを明示している。機能面では代替できても、指定技術スタックには不適合。 |
| WebGL、固定時間、論理と描画の分離 | **部分対応** | `GameWorld`は30Hz固定更新、`scene.ts`はスナップショット描画。 | 画面状態や一部ロジックがUI／ワールドに分散。固定時間の核は良い。 |
| 斜め固定カメラ・巨大盤面・暗い空間 | **達成** | `ArcRotateCamera`、細長い足場、暗色空間。 | 衝突時だけの弱い揺れ、ステージ別材質変化は未実装。 |
| NORMALの捕獲と取り逃し | **達成** | `handleFalling`でmiss、MARK一致で捕獲。 | 視覚演出は不足。 |
| VEILの3×3 AREAと連鎖 | **部分対応** | `areaTargets`と`captureCube`は複数AREA・VEIL追加を処理。 | AREAが実際に連鎖するプレイ、範囲の同時演出、生成器での利用、テストが不足。 |
| VOIDを残す・MARK上保護 | **部分対応** | `areaTargets`がMARK上VOIDを除外。 | チュートリアルがこの必須戦術を実演付きで教えない。視覚上の亀裂・警告記号も弱い。 |
| 青いMARKの1個制約 | **達成** | `marker: GridPosition | null`。 | 足元の選択床ハイライト、捕獲時の強い発光・沈下・粒子がない。 |
| 捕獲停止時間を移動に使う | **部分対応** | `CAPTURE_PAUSE`でもプレイヤー移動を許可。 | 演出は停止時間だけで、キューブ沈下・粒子・光・吸引表現がない。 |
| 辺支点の90度転がり | **未対応** | 描画は `z + p` の前進と中心回転。 | 支点回転ではなく、浮き上がり＋自転＋平行移動に見える。中核体験の重大差分。 |
| 回転中通過体積の衝突 | **部分対応** | `checkRollCollision`でz進捗と距離閾値を使用。 | 回転体のスイープ体積ではないため、偽陽性・偽陰性のリスクが高い。 |
| プレイヤーの必要アニメーション | **未対応** | 待機／走行は簡易ボブ、CRUSHED姿勢のみ。 | 印設置、踏ん張り、落下、クリア動作、足元セル表示がない。 |
| 押し潰し後の損害とAGAIN | **部分対応** | non-VOIDをmissに加算し、同問を再読込。 | 残キューブを高速で進めない。VOIDを含む「残り全種類」を取り逃し扱いにしない。落下・崩壊演出がない。 |
| 足場増減・ゲームオーバー | **部分対応** | miss上限、VOID捕獲、PERFECTで列数を変更。 | 列を即時に削除するだけで、後方崩壊・破片・プレイヤー落下の表現がない。 |
| PERFECTと規定回転数 | **部分対応** | 完全回収、VOID未捕獲、miss 0、報酬帯は実装。 | 回転数は最初の捕獲からではなくゲーム開始から増加し、最後の必要捕獲で止めない。要件と異なる。 |
| 得点・MIND INDEX | **部分対応** | 100/200点と3段階PERFECTは実装。 | 残り足場ボーナスは最終到達時のみ。HUD／結果のMIND INDEXはmiss減点を含まない一方、`GameWorld.mindIndex`は含む。二重定義で不整合。 |
| 9ステージ・88問・指定サイズ | **達成** | `stagePlan`は指定個数・指定サイズを生成。 | 問題の複雑性と難度曲線は後述の通り未達。 |
| Finalの補助減少 | **未対応** | Final専用の分岐がない。 | 床選択、AREA常時表示、捕獲補助、暗さの抑制がステージ9で変化しない。 |
| 解法逆算・ヘッドレス全問検査 | **部分対応** | 88件にsolutionとseed、`validatePuzzle`あり。 | 実GameWorldを再生せず、移動時間・AREA・VOID保護・衝突・失敗を検証しない。登録手順は単調。 |
| 各ステージの手作業代表問題 | **未対応** | `featured: ordinal === 1`のみ。 | 人間プレイで特徴確認した証跡や、手作業で設計した内容がない。 |
| 5難易度 | **部分対応** | 5設定が速度・停止・捕獲時間・移動速度を変更。 | BEGINNER予告、補助表示、Final以外の設置補助がない。EXTREMEの公平性検査もない。 |
| TUTORIAL | **未対応** | ヒント文字列と実戦開始のみ。 | 8段階の停止型学習、指定操作のゲート、保護・足場崩壊の実演、完全練習問題がない。 |
| CAMPAIGN自動保存・復帰 | **未対応** | `saveCampaign`は一部値を書き込む。 | 起動時に読まない。stage/wave/ordinal/miss/AREA等を復元しない。最高記録もない。 |
| PRACTICE補助 | **部分対応** | 一手送り、履歴、保存／読込の入口がある。 | 解答手順の段階表示がない。履歴を問題切替で消さない。無効なStage/Wave/Puzzle選択は最初の問題へ黙ってフォールバックする。 |
| CREATE | **未対応** | 塗り分け、保存、書出し、テストボタンはある。 | JSON読込、左右反転、規定回転数入力がない。作成解法が固定で検証に合わず、VALIDになりにくい。 |
| DUEL | **部分対応** | 交互処理、5点・2点差の内部ロジックがある。 | プレイヤー交代UI、途中スコア、結果画面、E2Eがない。実際に最後まで遊べた証跡がない。 |
| PC・ゲームパッド | **部分対応** | WASD／矢印／Space／Z／X／E／C／Shift／Esc、左スティック等に対応。 | Enter決定、戻るEsc、D-pad、明示的なコントローラ案内が不十分。 |
| モバイル | **部分対応** | 仮想スティック、AREA、FAST、MARK、safe-area、縦横レイアウトがある。 | 複数指ロック、長押しメニュー、ピンチ、FASTのボタン外リリースの対策が不十分。操作実効テストは可視確認のみ。 |
| 必要画面 | **部分対応** | タイトル、モード、難度、ゲーム、Pause、結果、設定、CREATE等はある。 | 読み込み画面なし。実質的なDUEL結果、詳細なWave／Stage結果、結果操作のEnter対応が不足。 |
| HUD | **部分対応** | SCORE/STAGE/WAVE/PUZZLE/ROLL/LOSS/ROWS/VEIL/難度/Pauseを表示。 | モバイルで簡略化されず、MIND INDEXの式がゲーム本体と不一致。 |
| 映像・演出 | **部分対応** | 暗い空間、色区別、影、Glow、簡易回転・着地音。 | 粗面、亀裂、粒子、砂ぼこり、破片、沈下、崩壊、落下、ステージ別材質、弱い揺れが未実装。 |
| 音楽・効果音 | **部分対応** | Web Audioの効果音パレット、ユーザー入力解除がある。 | 60〜90秒の環境音／音楽、足場追加・落下等の網羅、設定UIとの接続がない。 |
| 性能対策 | **部分対応** | low shadow、InstancedMesh、非表示時Pause、品質推定。 | quality保存値を描画へ反映しない。DPR上限・FPS観測・カリング・プール・実機性能証跡がない。 |
| デバッグモード | **部分対応** | `?debug=1`、座標、状態、STEP、AUTO、+ROW。 | 直接問題選択、衝突範囲、速度変更、PERFECT条件、問題検証結果、FPS・描画回数がない。 |
| Vitest | **部分対応** | 13件が基本ルールと88件読込を検査。 | 要求された多数の状態遷移・保存・PERFECT・押し潰し・回転数開始終了を未検査。 |
| Playwright／目視 | **未対応** | Chromiumの3テスト、3ビューポートのスクリーンショット。 | 要求操作12種の大半、横／小型端末、WebKit、実ゲームプレイ・演出同期確認を満たさない。 |
| 公開URL | **保留（ユーザー指示）** | 公開を保留する指示を受けた。 | 元要求の公開URL・公開版スクリーンショットは未提出。再開時に別途実施が必要。 |

## 4. 重大度別の技術的問題

### P0 — コアゲーム体験を変えている問題

| 問題 | 根拠 | 影響 | 推奨修正 |
| --- | --- | --- | --- |
| **転がりが辺支点回転ではない** | `scene.ts:255-258`は`z + p`の移動と中心回転。 | 巨大な立方体の重量、将来位置の読み、接触の直観が崩れる。 | 手前下辺を原点にしたpivot `TransformNode`を作り、90度回転後に整数セルへスナップする。 |
| **回転中衝突が近似しすぎ** | `GameWorld.ts:158-162`は距離閾値のみ。 | 見た目と判定のズレ、理不尽な死亡、回避できない／当たらない状態を生む。 | 回転区間ごとのswept AABB、またはpivot回転したキューブの危険領域を純粋関数で計算する。 |
| **規定回転数が要件定義と違う** | `rotations`は回転開始ごとに増えるが`hasScoringStarted`は未使用。 | PERFECT帯と得点の公平性が崩れ、問題データの規定値も意味を失う。 | 最初の必要キューブ捕獲時に計測開始し、最後の必要キューブ捕獲時に凍結する専用カウンターへ分離する。 |
| **問題検証が実プレイを保証しない** | `puzzleValidation.ts:40-55`は固定targetZで手順を照合するだけ。 | 「88問すべて解ける」の保証が成立しない。 | GameWorldのヘッドレスモードを作り、移動入力・MARK・AREA・VOID・衝突・落下を固定時刻で再生する。 |

### P1 — モード・保存・品質保証を壊す問題

| 問題 | 根拠 | 影響 | 推奨修正 |
| --- | --- | --- | --- |
| **CREATEのVALIDATEが実質的に成立しない** | `GameShell.tsx:105`の解法は`z:3`固定、検証器は`targetZ`を要求。 | 作った問題を検査して遊ぶ要求を満たせない。 | 作成盤面から解法を探索するか、少なくとも解法入力UIと可変targetZを追加する。 |
| **Campaign保存を読まない** | `GameWorld.ts:409-414`は書込みのみ。 | 「途中経過を自動保存し復帰」の要件を満たさない。 | 起動時にversioned saveを読込み、stage/wave/ordinal/difficulty/score/rows/missesを復元する。 |
| **設定が描画・音へ未接続** | `SettingsPanel`はlocalStorageへ書くが、`detectQuality`はそれを読まず、`AudioManager.setEnabled`も呼ばれない。 | ユーザーに見える設定が実機能を持たない。 | 設定コンテキストを作り、起動時に品質と音量をscene/audioへ注入する。 |
| **TUTORIALがゲート型でない** | `updateTutorial`は移動・捕獲数から文言を選ぶだけ。 | 初学者が保護や失敗の仕組みを学べず、元要件の8段階に適合しない。 | 専用の8小問題、入力ロック、成功条件、次段階遷移をデータ駆動で実装する。 |
| **DUELの体験が不完全** | 内部スコアはあるがHUD・結果専用表示がない。 | 交代制対戦の可視性と終局確認ができない。 | Player 1/2、現在手番、ラウンド結果、5点／2点差の専用画面とE2Eを追加する。 |

### P2 — 要求との差が明確なUX・演出・入力の問題

現行版では粒子、沈下、崩壊・落下、砂ぼこり、足場破片、ステージ別材質、明確なVOID亀裂、足元セル、キャラクター行動、Finalの視認補助低下が不足しています。これらは単なる装飾ではなく、キューブの種類・捕獲状態・足場損失を読み取るフィードバックなので、プレイ可能性にも影響します。

また、FASTは`pointerdown`／`pointerup`／`pointercancel`を使いますが、ボタンにpointer captureがなく、指をボタン外で離すケースを確実に解消できません。複数指入力、長押しコンテキストメニュー、ピンチズームも積極的には遮断していません。モバイル要件は「表示できる」ではなく「入力が残らない」ことまで求めているため、実機操作での再検証が必要です。

### P3 — 技術仕様・提出物・保守性の問題

Three.js指定に対してBabylon.jsを採用している点は、明示的な技術要件との差分です。Babylon.js自体が悪いわけではありませんが、要件準拠を主張するならThree.jsへ移行する必要があります。加えて、外部依存の`PANEL`画像URLは画面に必須ではない装飾ですが、生成失敗時の代替設計をコード内に残しています。ビルドは成功しますが、Babylon本体の単一バンドルは圧縮後約476KBで警告が出ており、モバイル性能目標を実測で裏付けていません。

## 5. 問題生成・88問の品質監査

`generate-puzzles.mjs`は数とサイズを正しく生成します。しかし、解法から「複雑な盤面」を逆算する設計ではありません。必要キューブは偶数奥行の`safeRows`にしか置かれず、各必要キューブは同じ手順、すなわち「最手前セルで、1回前にMARK、次回にCAPTURE」へ収束します。VOIDはランダムに置かれますが、解法にVOID保護やAREAを組み込まないため、元要件の核心である順序設計・保護・連鎖・逃げ道は問題として検証されていません。

この状態で`featured: ordinal === 1`を付けても、「各ステージに人間が遊んで特徴確認した代表問題」という条件を満たしません。データの有無と、ゲームデザイン上の88問の有効性は別です。

| 監査観点 | 現状 | 判定 |
| --- | --- | --- |
| 88件、固有ID、seed、サイズ | 生成器で担保。 | 達成 |
| 同seedの決定性 | LCGで再現可能。 | 達成 |
| 盤面重複・境界外 | validatorで確認。 | 達成 |
| 登録手順の基本照合 | markerと非VOIDの一致を確認。 | 部分対応 |
| 標準速度での移動可能性 | 未検証。 | 未対応 |
| AREA連鎖の解法 | 未生成・未検証。 | 未対応 |
| VOID保護の解法 | 未生成・未検証。 | 未対応 |
| Crush・miss・game-overの失敗経路 | 未検証。 | 未対応 |
| 手作業代表問題 | フラグのみ。 | 未対応 |

## 6. テストの信頼度評価

Vitestの13件は、`markerCanCapture`、3×3選択、VOID保護、得点帯、JSON読込などの純粋関数に限る有用なスモークテストです。ただし、`GameWorld`の進行全体を通すテストではありません。要件にあった「青印が一つだけ」「VEIL捕獲後のアンカー」「PERFECTで足場増加」「VOIDの正常落下」「押し潰しの残数処理」「規定回転数開始・終了」「Campaign保存復元」「巻き戻し再現性」は未テストです。

Playwrightは3件だけです。Campaign開始とPause、CREATEのlocalStorage書込み、モバイル操作ボタンの可視性を確認していますが、チュートリアルの完了、実際の捕獲とAREA、ゲームオーバー再挑戦、設定反映、CREATE再読込、DUEL終局、縦・横・小型端末での**操作**は検査していません。要件上の12操作と4種以上の画面幅に対し、現行の証跡は不足しています。

> **重要:** 「テストがPASS」は「テスト対象がPASS」を意味するだけです。現行テストは要求全体の合格証明ではありません。

## 7. 再現性の高い不具合・欠落の再現手順

| ID | 再現手順 | 期待 | 実際 | 重大度 |
| --- | --- | --- | --- | --- |
| R-01 | `?debug=1&demo`でキューブを観察する。 | 手前下辺を支点に90度で倒れ込む。 | 中心回転とz移動に見える。 | P0 |
| R-02 | CREATEで任意のセルを塗り、VALIDATEを押す。 | 解ける構成なら有効判定または妥当な警告。 | 固定`z:3`解法と検証器のtargetZ前提が衝突し、妥当性が不正確。 | P1 |
| R-03 | SETTINGSでAUDIO=OFF、QUALITY=LOWを選び、再開する。 | 音が止まり品質が反映される。 | 保存値のみ変わり、AudioManagerとscene品質に接続されない。 | P1 |
| R-04 | Campaignを進め、再読み込みする。 | 中断位置・難度・得点・足場が復元される。 | 保存を読み込む処理がない。 | P1 |
| R-05 | PRACTICEでFinal、Wave 3、Puzzle 3を選ぶ。 | 存在する範囲に制限するか明確にエラー。 | `findPuzzle`失敗時に先頭問題へ黙って戻る。 | P2 |
| R-06 | モバイルでFASTを押し、ボタン外で指を離す。 | FASTが必ず解除される。 | button自身へpointer captureがなく、解除イベントを取りこぼす可能性がある。 | P2 |
| R-07 | HUDのMIND INDEXと最終結果を、missがある状態で比較する。 | 同一の式・数値。 | `GameWorld`式とUI式が異なり得る。 | P1 |

## 8. 優先度付き修正ロードマップ

### 第1スプリント — プレイの正しさを回復する

まず、`GameWorld`から純粋な「roll transform / swept collision / scoring window」モジュールを切り出します。これを使って、Babylon側をピボット回転へ変更し、衝突を同じ幾何学計算で判定します。次に、規定回転数を最初の必要捕獲から最後の必要捕獲までの専用値へ変更し、MIND INDEXを単一関数に集約します。この段階が完了するまで、ステージ数・演出・公開を追加してはいけません。

### 第2スプリント — 88問を「解ける問題集」にする

問題解法を先に生成し、プレイヤーの移動速度、MARKの単独制約、捕獲Pause、AREA、VOID保護を含むヘッドレスGameWorldで再生します。失敗するseedは棄却します。さらに各ステージに1問ずつ手作業の代表問題を置き、解法・狙い・実プレイ確認者・確認日をJSONメタデータに記録します。CREATEには、インポート、ミラー、規定回転数、解法入力／探索、正しいVALIDATEを実装します。

### 第3スプリント — モードと保存を本物にする

TUTORIALを8つの小問題と操作ゲートへ置換します。Campaignをversioned saveにし、起動時復元と最高記録を追加します。PRACTICEには解法ステップ表示と問題切替時の履歴リセットを追加します。DUELには手番表示、得点板、ラウンド結果、終局画面を追加します。

### 第4スプリント — 入力・演出・性能・受入試験

モバイルのpointer captureとmulti-touch排他を導入し、contextmenu／gesture／viewport制御を追加します。次に、捕獲沈下、粒子、崩壊、落下、VOID亀裂、床材変化、足元ハイライト、Final可読性調整、環境音を追加します。最後に、必須単体テスト、12操作のPlaywright、400×870、870×400、小型端末、WebKit、実機FPS計測を通します。

## 9. 監査後の完成判定

**現状は「公開可能な完成版」ではありません。** 公開を保留したことはユーザー指示に沿っていますが、公開可否を除いても、元要件の完成条件には未達が残ります。特にP0とP1を解消し、テストを拡張するまでは、外部への完成報告を「実装済み」ではなく「検証中のプロトタイプ」と訂正することを推奨します。

## 10. 証跡一覧

| 証跡 | 用途 |
| --- | --- |
| `Pasted_content.txt:106-127, 200-260, 315-360, 401-460, 464-599, 603-759, 811-946` | 元要件の技術、ゲームルール、モード、検査、完成条件。 |
| `client/src/game/GameWorld.ts:73-429` | 固定更新、進行、捕獲、失敗、保存、練習、デバッグ。 |
| `client/src/game/scene.ts:26-270` | WebGL描画、カメラ、回転表現、品質推定、InstancedMesh。 |
| `client/src/components/GameShell.tsx:14-132` | タイトル、設定、モード、CREATE、HUD、タッチ操作。 |
| `scripts/generate-puzzles.mjs:3-52` | 88問の生成ロジックと解法の単調性。 |
| `client/src/game/puzzleValidation.ts:12-55` | 現行の問題検証範囲。 |
| `client/src/game/AudioManager.ts:4-49` | 音声の実装と設定未接続。 |
| `client/src/game/rules.test.ts` | Vitest 13件の検査範囲。 |
| `e2e/cubic-ordeal.spec.ts:3-36` | Playwright 3件の検査範囲。 |

