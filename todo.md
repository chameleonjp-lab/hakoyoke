# 要件差分監査チェックリスト

- [x] 元要求の全32節を監査項目へ正規化する。
- [x] 現行ソース、問題JSON、テスト、README、検査レポートの証跡を収集する。
- [x] タイトル・ゲーム中・モバイル・デバッグ画面を再確認する。
- [x] 実装済み、部分対応、未対応、根拠不足を機能別に分類する。
- [x] ゲームロジック、問題生成、UI、入力、保存、音、性能、テストのリスクを採点する。
- [x] 重要度・修正難易度・依存関係を含む修正ロードマップを作成する。
- [x] 根拠付きの日本語レビュー報告書を提出する。

# P0・P1 改修チェックリスト

- [x] 回転支点と回転体積を表す純粋な幾何モジュールと単体検査を追加する。
- [x] Babylon描画を手前下辺支点の90度回転へ変更する。
- [x] GameWorldの回転中衝突と規定回転数計測を改修する。
- [x] 88問をヘッドレスの実ゲーム規則で再生検証する。
- [x] CREATEへ規定回転数、左右反転、JSON読込、解法検査を実装する。
- [x] TUTORIALを8段階の操作ゲート型に改修する。
- [x] DUELへ手番・得点・結果の表示を追加する。
- [x] Campaignの保存読込と最高記録を実装する。
- [x] quality/audio設定を3D描画と音声へ接続する。
- [x] VitestとPlaywrightを拡張し、デスクトップ・モバイルで回帰検査を実施する。

# 盤面座標・移動方向修正

- [x] 床タイルの上面高さとキューブのローカル原点の差分を特定する。
- [x] 論理Zを奥から手前へ減少させる座標規約に統一する。
- [x] 手前向き辺支点の90度回転と衝突スイープを新しいZ規約に合わせる。
- [x] 88問の解法・検証器・デモを新しい奥行方向へ再生成する。
- [x] デスクトップとモバイルで床上のキューブと移動方向を目視確認する。

# プレイヤー落下ゲームオーバー

- [x] プレイヤーの連続座標が床タイル範囲を外れた瞬間を検出する。
- [x] 落下中は入力・衝突を停止し、ゲームオーバーへ遷移する。
- [x] 盤外移動・盤外落下・ゲームオーバーを単体検査とブラウザ検査で確認する。

# 代表問題・演出・実機寄り検査

- [x] AREAでVEILを連鎖捕獲し、MARK上のVOID保護を必須にする代表問題を設計する。
- [x] 代表問題の解法をヘッドレス再生し、連鎖・保護・規定回転数を検査する。
- [x] 捕獲時の沈下、信号粒子、床面発光を追加する。
- [x] 足場崩壊時の段階的な列消失、破片、カメラ揺れを追加する。
- [x] 870×400横画面でタッチ入力・HUD・盤面をブラウザ検査する。
- [x] WebKitで主要な開始・入力・落下ゲームオーバー操作を検査する。

# モバイル縦画面・入力方向・視認性の再設計

- [x] タップ開始位置に出現し、指の移動で追従するフローティング移動キーを実装する。
- [x] 入力ベクトルと画面上のキャラクター進行方向を統一する。
- [x] キューブを有効盤面の奥端から出現させるデータ・描画規約へ統一する。
- [x] タイル境界、盤面外縁、キューブ輪郭、プレイヤーのコントラストを強化する。
- [x] 375×812の縦画面でHUD、フローティング移動キー、主要アクションを再配置する。
- [x] ChromiumとWebKitの縦画面操作で入力方向・奥端出現・可読性を検査する。

# GitHub完全移行

- [x] GitHub認証、指定リポジトリの所有者、既存履歴、既定ブランチを確認する。
- [x] アプリ本体、生成済み問題データ、テスト、設定、設計・監査・検査ドキュメントを移行対象として棚卸しする。
- [x] 実装した全ファイルを対象に、依存関係キャッシュ・実行時ログのみを除外する。
- [x] 指定GitHubリポジトリへ安全にコミット・プッシュする。
- [x] GitHub上のファイル構成、コミット、実行手順を検証する。

移行先は `chameleonjp-lab/hakoyoke` の `main` ブランチである。実装・設定・設計資料・テスト・88問の問題データ・生成済み独自素材原本を同期済み。最終検証では、リモートツリー123ファイル、新規クローンでの `pnpm install --frozen-lockfile`、Vitest 21件、Playwright 14件（Chromium/WebKit）、型検査、本番ビルドの成功を確認した。依存関係キャッシュと実行時ログのみをリポジトリから除外した。

# Babylon.jsバンドル最適化

- [x] 現行のBabylon.jsインポートとVite出力を分析し、巨大チャンクの根因を特定する。
- [x] 初期画面とゲーム3Dランタイムの読込を分離する最適化方針を設計する。
- [x] 深いモジュールインポートとRollupの手動チャンク設定を実装する。
- [x] 型検査、ユニットテスト、E2E、ビルド出力、ブラウザ表示で回帰を検証する。
- [x] バンドル警告の解消結果を記録し、GitHubへ同期する。

# 段階読込・RUM・依存関係最適化PR

- [x] Babylon.jsのテクスチャ・シェーダー読込を、プレイ開始後に優先度別で段階化する。
- [x] 初回起動のRUM指標を収集し、ブラウザで確認できる開発用観測面を追加する。
- [x] 実際に参照されていないテンプレート依存関係を監査し、安全に削減する。
- [x] 型検査、単体テスト、E2E、ビルド、ブラウザ表示、依存関係サイズを検証する。
- [ ] 最適化内容を専用ブランチでPull Requestとして提出する。
