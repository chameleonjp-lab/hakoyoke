# 実行時性能の最適化計画

## 目的

Babylon.jsを必要最小限のコア描画で即時に開始し、グロー・影・テクスチャなどの品質向上機能を最初の描画後に段階的に有効化する。初回ロードから最初の描画までの実測値を匿名のRUMイベントとして記録し、不要なテンプレート由来の依存関係とソースを削減する。

## 実装方針

レンダリングは、盤面・キューブ・カメラ・基本照明を最優先パスとする。最初のフレームが描画された後、ブラウザのアイドル時間を利用してGlowLayerとShadowGeneratorを初期化する。アイドル時間が利用できない環境では短いタイムアウトへ安全にフォールバックする。

RUMは、ナビゲーション開始、ゲームランタイム要求、Babylonシーン準備完了、最初の描画、Performance APIのFCP/LCP/CLS/入力遅延を収集する。個人識別子や操作内容は送信せず、最近の標本のみをlocalStorageへ保存する。`VITE_RUM_ENDPOINT` が設定されている場合だけ匿名JSONを送信し、未設定時はネットワーク要求を行わない。`?rum=1` では開発用の観測面に最新値を表示する。

依存関係整理では、実ゲームの到達経路に使われないテンプレートUI群・ページ・フック・コンテキストを削除し、該当する直接依存関係をマニフェストとロックファイルから取り除く。ゲームで必要なReact、Babylon.js、Buttonに必要な最小ユーティリティ、サーバー実行に必要なExpress、テスト・ビルド関連の開発依存のみを残す。

## 受入基準

タイトル、TUTORIAL、CAMPAIGN、PRACTICE、CREATE、DUEL、Campaign復帰、縦画面入力は従来どおり動作する。最初の3D描画は装飾的なグローや影の初期化を待たない。本番ビルドは500 kB警告を発生させず、型検査、Vitest、Playwright（Chromium/WebKit）が成功する。PRにはビルド出力、RUMの観測範囲、削除した依存関係を明記する。

## 実施結果

最初の描画では盤面、キューブ、カメラ、基本照明だけを初期化するように変更した。最初の描画後、アイドル時間にShadowGenerator、GlowLayerのシェーダー、玄武岩タイルのテクスチャを動的インポートし、ゲームプレイを妨げずに品質を向上させる。

`client/src/lib/rum.ts` は匿名のNAV、FCP、LCP、CLS、初回入力遅延、3Dランタイム要求、準備完了、最初の描画を観測する。最近24件までをlocalStorageに保持し、`VITE_RUM_ENDPOINT` が構成された場合のみJSONを送信する。`?rum=1` の観測面とE2EテストでRUM値の表示を確認した。

使用していないテンプレートUI、ページ、フック、コンテキスト、67件超の直接依存関係を整理し、production dependencyを `@babylonjs/core`、`express`、`react`、`react-dom` の4件に削減した。ロックファイルは7,241行から2,840行になった。最終ビルドの初期エントリは457.88 kB（gzip 129.22 kB）で、テクスチャは83.19 kBの遅延チャンク、GameCanvasは33.38 kBの遅延チャンクとして出力され、500 kB警告は発生しなかった。

型検査、Vitest 21件、Playwright 16件（Chromium/WebKit）、production buildが成功した。
